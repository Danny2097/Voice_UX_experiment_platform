server {
    listen 80 default_server;
    server_name _;

    # Enable gzip compression
    gzip on;
    gzip_types text/css application/javascript application/json;
    gzip_min_length 1000;

    # Logging to stdout/stderr for Docker
    access_log /dev/stdout;
    error_log /dev/stderr;

    # Serve static files from /usr/share/nginx/html
    root /usr/share/nginx/html;
    index index.html;

    # Allow large audio uploads (up to 500 MB)
    client_max_body_size 500M;

    # Suppress favicon 404 noise
    location = /favicon.ico {
        log_not_found off;
        access_log off;
        return 204;
    }

    # Static files with long cache (CSS, JS)
    location ~ \.(css|js)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # HTML files with shorter cache
    location ~ \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # Other static assets
    location ~ \.(json|woff|woff2|ttf|eot|svg|png|jpg|jpeg|gif|ico)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # ── Audio upload/download (long timeout, no buffering) ───────
    location /api/audio/ {
        proxy_pass             http://localhost:3002/api/audio/;
        proxy_http_version     1.1;
        proxy_set_header       Host            $host;
        proxy_set_header       X-Real-IP       $remote_addr;
        proxy_read_timeout     300s;
        proxy_send_timeout     300s;
        proxy_request_buffering off;
        proxy_buffering         off;
    }

    # ── Database API (internal port 3002) ────────────────────────
    location /api {
        proxy_pass         http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
        proxy_buffering    off;
    }

    # ── Standalone Local API (dedicated container port 3003) ─────
    # Generic proxy to local-api service
    location /local-api-proxy/ {
        resolver 127.0.0.11 valid=30s;
        proxy_pass         http://local-api:3003/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
        proxy_buffering    off;
    }

    # Backward compatibility for old routes
    location /local-api {
        resolver 127.0.0.11 valid=30s;
        set $upstream_local_api http://local-api:3003/api/objects;
        proxy_pass         $upstream_local_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
        proxy_buffering    off;
    }

    location /clothing-api {
        resolver 127.0.0.11 valid=30s;
        set $upstream_clothing_api http://local-api:3003/api/clothing;
        proxy_pass         $upstream_clothing_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
        proxy_buffering    off;
    }

    # ── Serve experiment_images ──────────────────────────────────
    location /experiment_images/ {
        alias /usr/share/nginx/html/experiment_images/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # ── CORS proxy for external museum APIs (internal port 3001) ─
    location /proxy {
        proxy_pass         http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        'upgrade';
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # HTML5 history mode: fallback to index.html for non-matching routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
